import 'package:flutter/material.dart';

class AppColors {
  // Main background colors
  static const Color primaryBackground = Color(0xFF1E1E2C); // Dark navy / main bg
  static const Color secondBackground = Color(0xFF2D2D44);  // Slightly lighter bg

  // Accent colors
  static const Color accent = Color(0xFFFFC107);            // Warm yellow
  static const Color accentLight = Color(0xFFFFD54F);       // Lighter yellow for hover/active

  // Text colors
  static const Color textPrimary = Colors.white;            // Main text
  static const Color textSecondary = Color(0xFFB0B0C3);     // Muted text

  // Button colors
  static const Color buttonPrimary = Color(0xFFFFC107);     // Main button background
  static const Color buttonText = Colors.white;             // Button text color

  // Borders / dividers
  static const Color border = Color(0xFF3A3A55);

  // Success, error, info
  static const Color success = Color(0xFF4CAF50);
  static const Color error = Color(0xFFF44336);
  static const Color info = Color(0xFF2196F3);

  static const Color black = Color(0xFF000000);
  static const Color white = Color(0xFFFFFFFF);
  static const Color grey = Color(0xFFB0B0C3);
  static const Color lightGrey = Color(0xFF3A3A55);
  static const Color darkGrey = Color(0xFF2D2D44);
  static const Color lightBlue = Color(0xFF2196F3);
  static const Color darkBlue = Color(0xFF1E1E2C);
  static const Color lightGreen = Color(0xFF4CAF50);
  static const Color darkGreen = Color(0xFF2E7D32);
}
