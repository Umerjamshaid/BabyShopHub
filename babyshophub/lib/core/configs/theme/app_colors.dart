import 'package:flutter/material.dart';

class AppColors {
  static const primary = Color(0xFFFFC107);          // Bright amber/yellow for buttons & highlights
  static const background = Color(0xFF121212);       // Dark background
  static const secondBackground = Color(0xFF1E1E1E); // Slightly lighter dark for cards/inputs
}
